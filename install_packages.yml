---

- hosts: db_servers
  tasks:
  - name: install mysql
    package: 
      name: 
        - mysql

- hosts: analysis_server
  tasks:
  - name: install python and node
    package:
      name:
      - "{{ python_package }}"
      - nodejs
      - mysql-client
